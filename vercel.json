{
    "version": 2,
    "builds": [
        {
            "src": "server/api/**/*.ts",
            "use": "@vercel/node",
            "config": {
                "nodeVersion": "24.x",
                "includeFiles": "../data/**"
            }
        }
    ],
    "routes": [
        {
            "handle": "filesystem"
        },
        {
            "src": "/api/(.*)",
            "dest": "/server/api/$1.ts"
        }
    ],
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/server/api/$1.ts"
        }
    ]
}